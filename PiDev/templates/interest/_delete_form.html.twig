<form method="post" action="{{ path('app_interest_delete', {'idIntrest': interest.idIntrest}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ interest.idIntrest) }}">
    <button class="btn">Delete</button>
</form>
