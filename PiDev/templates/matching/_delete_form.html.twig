<form method="post" action="{{ path('app_matching_delete', {'idMatch': matching.idMatch}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ matching.idMatch) }}">
    <button class="btn">Delete</button>
</form>
