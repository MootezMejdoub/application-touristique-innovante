<form method="post" action="{{ path('app_facture_delete', {'idfacture': f.idFacture}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ f.idFacture) }}">
    <button class="btn">Delete</button>
</form>
