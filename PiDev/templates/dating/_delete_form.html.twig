<form method="post" action="{{ path('app_dating_delete', {'idDate': dating.idDate}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dating.idDate) }}">
    <button class="btn">Delete</button>
</form>
