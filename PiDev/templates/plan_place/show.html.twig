{% extends 'basebaha.html.twig' %}

{% block title %}PlanPlace{% endblock %}

{% block body %}
    {{ include('my_side_bar_back_office.html.twig') }}
    <h1>PlanPlace</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Ref</th>
                <td>{{ plan_place.ref }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_plan_place_index') }}">back to list</a>

    <a href="{{ path('app_plan_place_edit', {'ref': plan_place.ref}) }}">edit</a>

    {{ include('plan_place/_delete_form.html.twig') }}
{% endblock %}
