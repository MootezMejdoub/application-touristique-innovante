<form method="post" action="{{ path('reservation_delete', {'numreservation': res.numReservation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ res.numreservation) }}">
    <button class="btn">Delete</button>
</form>
