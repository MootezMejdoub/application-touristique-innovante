{% extends 'base1.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idcomm</th>
                <td>{{ commentaire.idcomm }}</td>
            </tr>
            <tr>
                <th>Nomu</th>
                <td>{{ commentaire.nomu }}</td>
            </tr>
            <tr>
                <th>Prenomu</th>
                <td>{{ commentaire.prenomu }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_edit', {'idcomm': commentaire.idcomm}) }}">edit</a>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
